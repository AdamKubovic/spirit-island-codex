# 03 — Replace the "Element combinations" dot-matrix with a proper UpSet plot

Status: done
Category: enhancement

## What happened

The Dashboard's Minor/Major deck segments show an "Element combinations" view rendered as a hand-rolled dot-matrix (one row per element set, dots plus a count column). The reporter likes the tab overall, but the visual doesn't match the mindset of an UpSet plot — set sizes and intersections aren't visually comparable the way https://upset.js.org/app/ presents them.

## What I expected

An UpSet-style plot for element combinations: intersection-size bars over the combination dot matrix (and ideally per-element set-size bars), so the relative frequency of each element combination is readable at a glance.

## Steps to reproduce

1. Open Dashboard → Minor (or Major) deck segment.
2. Look at "Element combinations": counts are plain numbers next to dot rows, with no bar encoding of intersection sizes.

## Additional context

- Owner decision: **prototype both approaches first** before committing — (a) adopting the UpSet.js library (`@upsetjs/react`) vs (b) custom-building the UpSet layout on the existing hand-rolled JSX+CSS approach (the repo currently has zero charting dependencies; everything is dots and percentage-width bars styled via `deck-*` classes). Run through `/prototype`, keep the answer, then implement.
- The data layer already produces exactly what an UpSet plot needs (combination groups sorted by count, canonical element order), so this is expected to be a presentation-layer change only.
- Whatever wins should preserve the existing "Highlight my spirit" element-highlight behavior.

## Comments

> *This was generated by AI during triage.*

Triage outcome: **ready-for-human** — blocked on a design decision (maintainer chose "prototype both first"). Next step is a `/prototype` session comparing (a) `@upsetjs/react` vs (b) a custom UpSet layout on the existing hand-rolled JSX+CSS approach, fed by the real `combinations` data from the deck-composition domain module. Once a winner is picked, re-triage to `ready-for-agent` recording the decision (library or custom) and any layout choices. The "Highlight my spirit" behavior must survive either way.

**2026-07-21 — resolved, retroactively.** This report (08:54) was overtaken by `deck-dashboard`'s
own independent effort, which ran the exact `/prototype` session this ticket asked for and
shipped the result a few hours later (12:26, `deck-dashboard #03`, commit `11f894d`) — see
`.scratch/deck-dashboard/issues/03-power-deck-chart-vocabulary.md` and the map's decision log.
Owner picked prototype B, a custom-built UpSet (no `@upsetjs/react` dependency — the repo keeps
zero charting deps): intersection-size bars over the combination dot matrix
(`deck-upset-colfill`), per-element set-size bars (`deck-upset-totalfill`), colour-coded by
combination size, now live as `src/components/DeckUpset.tsx` in the Dashboard's Minor/Major
segments. "Highlight my spirit" survived (`deck-upset-rowlabel-highlight` /
`deck-upset-dot-highlight`, tested in `appSmoke.test.tsx`). No further work needed here; status
flipped straight to `done` without a separate re-triage step since the fix already exists.
